# 模型卡 / 模型验证报告 —— AI Fraud Investigation Copilot

> **文档定位**：这是一份**离线决策系统**的模型交付文档，参照模型风险管理（MRM）的组织方式撰写。
> **它不声称本系统曾在生产环境运行。**
>
> **阅读约定**：每个数字后标注 `[实测]` / `[假设]` / `[已作废]`，并标注版本基线。
> 未取得的数字一律留 ▢，**不得以估计值填充**。
>
> **常驻规矩（2026-08-05 定，双方各违反过一次）**：
> **本文件内任何数字都必须在 `reports/` 有出处**；无出处者**要么补跑、要么删除**，
> **禁止凭记忆填**。已发生的两次：总指挥写入 `+0.023` 时对应报告不存在（已补跑确认 +0.0234）；
> 执行方填 step-up 网格区间与漏斗数时凭印象写错（已按 `reports/stepup.md` 订正）。

| 项 | 内容 |
|---|---|
| 系统名称 | AI Fraud Investigation Copilot |
| 版本 | ML 核心 v1 · Agent 层 **v4-citable-context** |
| 文档日期 | 2026-08-05 |
| 负责人 | 项目 owner（单人开发） |
| 定位 | 离线决策系统 + 已推演工业化路径 |

---

## 1. 用途与边界

**预期用途**：对**已完成**的电商交易做欺诈风险评分；对被闸门标记的交易，由 Agent 生成带证据引用的调查报告与处置建议，供**人工复核拍板**。

**明确不适用**：
- 授信 / 额度决策（不同的目标变量与合规要求）
- 反洗钱（AML）监测
- 实时在途拦截（本系统为离线批处理，无实时特征服务）
- 任何自动执行的终局处置——**系统输出是建议，不是决定**

**人在环**：所有非"放行"档位的处置均需人工复核；复核结论回流，且系统对回流偏差有显式处理（见 §8）。

---

## 2. 数据

| 项 | 值 |
|---|---|
| 来源 | Kaggle IEEE-CIS Fraud Detection（Vesta 真实电商交易） |
| 规模 | 590,540 笔 × 434 列 `[实测]` |
| 欺诈率 | **3.499%**（20,663 笔，≈1:29）`[实测]` |
| 时间跨度 | 182 天（`TransactionDT` 为相对秒，非日历时间）`[实测]` |
| identity 覆盖 | 24.42% `[实测]` |
| 缺失 | 434 列中仅 20 列完全无缺失；12 列缺失 >90%；V 系列块状同缺 `[实测]` |

**数据的结构性限制（影响可迁移性，必须随结论一起说）**：
- **无细粒度设备指纹 / 账户 ID**：`DeviceInfo` 是操作系统字符串，不是设备指纹；邮箱域仅 59 个公共值。关联图只能用**组合键**构造（见 §3）。
- V/C 系列为匿名特征，语义不可知——这既限制了可解释性，也意味着**部分结构信号可能已被匿名计数吸收**（§4 有实证）。
- 数据为 2019 年电商交易，**不是银行核心系统数据**：无渠道、无账户层级、无征信、无黑名单。

**切分口径**：
- 训练 `[0, 125]` / 空窗 `[125, 146)` 丢弃 / 测试 `[146, 181]`
- **随机切分被明确禁止**：欺诈标签滞后到达（拒付窗口），随机切会用未来预测过去。
- **21 天 embargo**：模拟"最新交易的拒付标签尚未成熟、训练时拿不到"。
- Kaggle 官方 test 集无标签，**不参与任何线下评估**（仅用于一次外部提交，见 §5）。

---

## 3. 特征与防泄漏

**表特征**：LightGBM 原生类别与缺失处理；**无目标编码**（避免隐形泄漏）；缺失作为取值参与，不填均值。

**图特征（时间因果聚合）**：以 `card1`、`card1+addr1`、`card1+邮箱`、`card1+设备` 等**组合键**构造共享实体。
> 设计依据：团伙 = 共享了本不该共享的**稀有**实体。`P_emaildomain` 这类高频公共值若裸连，会形成巨型连通分量吞噬信号，故排除或仅在稀有取值时连边。

**两层防泄漏（本系统的核心时间纪律）**：

| 层 | 语义 | 实现 |
|---|---|---|
| **结构型**（`prior_cnt`、fan-out） | 只问"在不在本行之前" | 只用 t 之前的边；SQL 中为 `ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING`（位置语义） |
| **标签型**（prior 欺诈率） | 还要问"标签熟没熟" | 邻居标签留 21 天 embargo；SQL 中为 `RANGE ... 1814400 PRECEDING`（取值语义） |

**泄漏审计**：将 embargo 由 21 天拉长至 60 天，图特征增益 **+0.039 → +0.023** `[实测]`——**缩但不崩**，说明是真实的 repeat-offender 信号而非泄漏；缩水部分归因于新鲜度损失。

**双实现对账**：同一套特征另以 DuckDB + 小星型模型实现，15 个特征列与 pandas 版**逐行一致**（浮点 rtol=1e-9）`[实测]`；并跑了**负对照**（故意将 `prior_cnt` 改为 RANGE 帧 → 不一致行数 0 → 166），确认对账本身有判别力。

**已修缺陷记录**：组合键为 NA 时曾塌成单一巨型组（pandas 侧已修，各 NA 独立成组）；SQL 侧 `PARTITION BY` 默认视 NULL 相等，会重新塌回，已用 `COALESCE` 复刻。

---

## 4. 性能与工作点

**主模型**：LightGBM，自然类别分布训练（不重采样、不加权，理由见 §6）。

| 指标 | 纯表 | 表+图 |
|---|---|---|
| PR-AUC（主排序指标） | 0.5645 | **0.6032** `[实测]` |
| ROC-AUC（次要，仅对外可比） | 0.9138 | 0.9306 `[实测]` |

**时间外乐观 gap（+21 天 embargo）**：PR-AUC 0.5645 → 0.5323（**Δ −0.0322**）`[实测]`
**gap 归因（等量旧窗对照，N=64,449 对齐）**：数据量损失 **+0.0048（≈0）** / 新鲜度净损失 **−0.0370** `[实测]`
> 即：gap 几乎全部来自"拿不到最新标签"，不是"样本变少"。

**容量工作点**（按分数取 top X%，对应复核员日处理能力）`[实测]`：

| 容量 | precision | recall（=有效拦截率） |
|---|---|---|
| top 0.5% | 97.5% | 14.2% |
| top 1% | 89.7% | 26.2% |
| top 2% | 72.3% | 42.3% |

**代价敏感工作点**：c_FN = 交易金额、c_FP = 固定 $25 `[假设]` → 期望成本最优阈值 **t\* = 0.078**（非 0.5）`[实测]`
→ recall **0.338 → 0.623**、总成本 **−26.1%**、拦截量 5.47% `[实测]`
→ 敏感性：c_FP ∈ {10, 25, 50, 100} 时 t\* = {0.036, 0.078, 0.177, 0.272}，节省 {44%, 26%, 15%, 8%} `[实测]`
→ 逐样本金额感知阈值 t_i = c_FP/(a_i + c_FP)：总成本 $254,317 < 最优全局 $299,499 `[实测]`

**图增益的成分**（分组消融）`[实测]`：增益**几乎 100% 来自实体标签历史**；度 / fan-out 结构特征贡献 ≈ 0（`fanout` 组 −0.0008）。
> 结论：结构信号已被匿名 C/V 计数吸收 → **GNN 对照臂（GTAN）放弃且未跑**，以量化依据替代实验。
> **但同一特征换目标后结论相反**，见 §7 网络项效度。

**外部可复核信号**（Kaggle late submission，提交号 55152570，2026-08-01）`[实测]`：
- **private LB 0.913711**（public LB 0.939134）
- **百分位：公开榜 3,551 / 6,355 = 前 55.9%**，略低于公开榜中位 0.941266
  → ⚠️ **私榜百分位无法报出**：`leaderboard --download` 对本赛题只返回公开榜，故只报私榜分数本身、不臆测名次。
- **训练口径：全量 train 重训**（非线下的时间切分版）；轮数沿用线下已定值，**不在 test 上调任何东西**。

> **两套口径必须分开读**：线下 = 时间切分 + 21 天 embargo（目的是**不骗自己**）；
> 提交版 = 全量重训（目的是**让别人能复核**）。线下 ROC-AUC 0.9306 与私榜 0.9137 **不是同一个数**，不可互替、也不矛盾。
>
> **定位**：中游，且**提前压过预期**——未做任何刷榜向 FE（无 UID 聚合 / magic feature / 融合 / test 分布适配）。
> **价值不在名次，在于这是项目里唯一由第三方用我看不到的标签算出来的数。**
>
> **附带实测的叙事红利**：下载后量得 train 覆盖 day 0–181、test 覆盖 **day 212–394**——
> **test 完全位于 train 之后且中间隔约 31 天**，与本项目「时间切分 + embargo」的结构天然一致。
> 防泄漏设置不是为了报告好看，它和比赛的真实时间结构是对齐的。
>
> **一处观察（含推断，已标注）**：public 0.939 → private 0.914 掉 0.025。若公私榜按时间切分，
> 这就是又一次「新鲜度衰减」，与线下 embargo 实验方向一致；**但我未核实该赛题公私榜的切分规则**，故记为观察不是结论。

---

## 5. 概率校准

| 指标 | 值 |
|---|---|
| 全局 ECE | 0.0071 `[实测]` |
| Brier | 0.0212 `[实测]` |
| **决策区间 top2% 校准 gap（raw）** | **0.002** `[实测]` |
| Platt / Isotonic 后同一 gap | 0.089 / 0.067 `[实测]` |

**结论：不做 blanket 后验校准。**
理由：LightGBM 以 logloss 训练即在优化 proper scoring rule，全局已较准；而**全局 ECE 在 3.5% 不平衡下被 p≈0 的易负样本稀释**，照不到真正做决策的 top 1~2% 区间。实测在短窗上套后验校准会**恶化稀疏高分尾**（全局 ECE 反而下降 0.0084→0.0058，是典型的聚合指标误导）。
**排除法**：近窗 14 / 28 / 42 天 → top2% gap 0.067 / 0.039 / 0.056 `[实测]`；42 天含 4.8k 正样本仍压不到 raw → 结论不是小样本假象。

**采用的策略：监控触发式重校准。** 平时不动，以决策区间可观测失准为触发器，在时间外近窗重校。
**支持证据**：停训 21 天后 ECE 0.0071→0.0096、Brier 0.0212→0.0221 `[实测]`——漂移会打坏校准，故重校准应由监控 gate，而非定期仪式。

---

## 6. 类别不平衡的处理

**处理方式：不处理**（自然分布训练，既不重采样也不用 `scale_pos_weight`）。

**依据（反例消融，同配置三组）** `[实测]`：

| | recall@1% | recall@2% | 全局 ECE | top2% 预测概率 |
|---|---|---|---|---|
| 自然分布 | 0.262 | 0.425 | 0.0078 | 0.697（实际 ≈0.69） |
| scale_pos_weight | 0.260 | 0.401 | 0.0871 | 0.931 |
| 随机过采样 | 0.258 | 0.400 | 0.0923 | 0.925 |

→ **动先验换不来排序，却把概率质量毁掉（ECE 11–12×）**；而本系统的代价敏感阈值与四档成本框架全部依赖真概率。
→ SMOTE 在含 NaN 与 31 个类别列的数据上本体不适用（附加论据）。

---

## 7. 决策框架（四档期望成本 argmin）

**动作空间**：放行 / 挂起（人工复核）/ 拒绝 / 上报（建档并评估冻结实体）。

```
E(放行)  = p · 金额
E(拒绝)  = (1 − p) · c_FP
E(挂起)  = c_review + p · 人工漏检率 · 金额 + (1 − p) · 人工误拦率 · c_FP
E(上报)  = c_report + 更低残余 − P(团伙) × 预期未来暴露
```
- **p 使用 raw GBDT 概率**（由 §5 的决策区间验证背书，非后验校准输出）。
- **未来暴露** ≈ 关联实体 fan-out × prior_fraud_rate × 中位金额，且**该项自身卡两层时间边界**（结构型只用 t 前边、标签型留 21 天）。

**参数（全部为假设，全部进敏感性扫描）** `[假设]`：c_FP $25（$10–100）· c_review $5（$2–20）· c_report $40（$20–100）· 人工漏检率 10%（5–20%）· 上报残余 5%（2–10%）

**成本结果（保守口径：上报的未来收益记零、成本照记）** `[实测]`：
全放行 $51,412/万笔 → 仅放行/拒绝两档 $21,921 → **四档（k_future=0）$15,278**（挂起档贡献主要降幅）；含网络项时 $25,048。

**网络项的效度验证**（本框架中唯一一个曾未经验证的前提）`[实测]`：
> 正确的锚不是本笔标签，而是"**高 gang 实体在其后续 30 天的欺诈率**"——因为上报的价值是冻结实体、阻断未来作案。
> 结果 lift **4.6–5.0×**（两个不重叠时间窗独立复制：t0=146 → 4.95×、t0=115 → 4.56×，CI 均不跨 1；留一实体后 4.66×）。
> **关键对照必须在"同样有欺诈史"的实体内部做**——`gang>0` 在构造上就要求实体已有 ≥2 笔确认欺诈，与全体比是同义反复。
> 成分拆解：密度效应 3.60–5.53× / **fan-out 效应 1.72–2.64×**（在 `prior_cnt` 分层内池化后仍 3.15×，排除"大实体设备多"的暴露量假象）。
> **⚠️ 得到验证的是前提的方向，不是收益量级**（本测量回答"后续欺诈率高几倍"，未回答"冻结能拦下多少钱"）→ **保守记账继续保留**。

> **与 §4 的表面矛盾及其解释**：fan-out 对"**本笔**是否欺诈"增量 ≈0（已被匿名特征吸收），对"**该实体未来**是否再作案"有 1.72–2.64×。**同一特征、不同目标、相反结论**——而网络项要的正是后者。

**第五档 step-up（加验证 / OTP / 3DS）已补齐** `[实测·假设参数]`（2026-08-05，`reports/stepup.md` + 图 13）：

```
E_stepup = c_friction + p·(1−r_block)·amount + (1−p)·r_abandon·(margin_rate·amount)
```
- **四个参数全部 `[假设]`**（本数据无「加验证后是否通过/放弃」的结局记录）：
  c_friction $0.50（$0.10–2.00）· r_block 0.70（0.50–0.90）· r_abandon 0.05（0.02–0.15）· margin_rate 0.15（0.05–0.30）。
- **结果**：基准参数下 step-up 占 test 窗 **10.6%**；**全网格 81 组区间 0.0%–55.8%（中位 8.1%）**
  → **区间极宽，只能报区间、绝不可报点值**（参数敏感性远大于层1 一致率那次的 15pp）。
  人工复核队列 6,946 → 3,916（**−44%**，基准参数下）。
- **⑧ 漏斗红利**：step-up 是自动动作、不需 Agent 也不占人工队列 →
  需要 Agent/人工的交易从 **9.7% 降到 6.7%**（10,937 笔转为自动加验证）。
- ⚠️ **预注册预测失败且照实记**：预测「step-up 吃掉 hold 与 decline 中间带」，
  实际 **72% 来自 `approve`**。机制：step-up 是五档中最便宜的干预（$0.50 vs hold $5），
  故它扩张的是 `approve` 的上边界；把 c_friction 提到 $2.00 后主要来源确实变回 `hold`（52%）——
  **「从谁那里吃」是相对价格的函数，不是它的固有属性。**
- **口径**：本实现**不修改**四档模块，五档并列呈现、不覆盖；既有全部数字仍以四档为准。

**规则与模型的分工（已量化）** `[实测]`（`reports/rules_vs_model.md`）：
15 条准入规则并集覆盖 test 窗 **40.0%** 流量、集内欺诈率 6.69%（**并集 lift 仅 1.95×**）。
**同容量对照**（N = 规则命中量 41,088）：模型独抓欺诈 **666 笔 / $129,000**，规则独抓 **74 笔 / $7,940**；
**规则所抓欺诈的 97.3% 模型也抓到了**。
> **并集在稀释精度**：单条 lift 范围 1.58×–6.10×，**并集 1.95× 低于 15 条里的 12 条**——
> 最宽的 `R_card6_credit` 一条就覆盖 23.2% 流量而 lift 仅 1.85×。
> → 规则应**分层用**（利的几条单独走高优先级队列），不是一个大 OR。
> → 规则的价值**不在增量召回**，在**可解释/可审计、冷启动兜底（⑨ 降级报告正是靠它）、可当天干预**。

**其余已知缺口**：velocity 规则家族（同实体短窗笔数）尚未实现 `[未开工]`；
step-up 的四个参数**无结局数据可标定**，只能报区间。

---

## 8. 已知局限与偏差（本节请勿删减）

1. **选择性偏差（结构性，无法完全消除）**：只有被拦下复核的样本才拿得到标签，放行的欺诈不可观测。演示：朴素「放行=legit」自训练 3 轮 test ROC **0.872→0.848（−0.024）**，每轮约 1,774 笔真欺诈被误标；加 **2% 随机放行抽检**后回到 0.866（**救回 +0.018**）`[实测]`。→ 建议保留固定探索预算做无偏锚定。
2. **成本参数全部为假设**，无本数据支撑；结论一律以敏感性区间形式给出。
3. **Agent eval 集的有效样本量小**：n_eff(Kish) = 29.4，单笔交易可占 68% 的成本质量；**金额口径的缺陷排序经 jackknife 判定不可用（已作废）** `[已作废]`。故 Agent 侧一律**报方向与排序，不报点值**。
4. **人工标注规模极小**：owner 亲手盲标 17 条，**标注者校准度未单独测量**（few-shot 自测未做）→ 该批产出定位为**缺陷分类学**，不产出任何比率。
5. **软层（LLM-as-judge）已整支作废** `[已作废]`：参照方为另一 LLM，且判定标准与 few-shot 出自同一参照方，分歧下降是构造保证的；少数类仅 3 条，判别力不可测。
6. **处置一致率不可作单点报告**：432 组参数网格上区间 **51–66%**，100 笔中 40 笔的应然档随参数改档 `[实测]`。① 之后该指标停用并交班给证据层一致性。
7. **数据不含设备指纹 / 征信 / 黑名单**，跨实体关联能力受限（见 §2）。
8. **属性层接地未测**：数字与 evidence_id 的编造可用代码判定（实测为 0），但"属性层断言是否有据"无机械判据。
9. **版本断层**：Agent 侧指标取自 **v3 证据池**；其后的 **v4-citable-context**（缺席事实与成本假设纳入可引用证据）改变了证据池，**跨版本数字不可直接比较**。

---

## 9. 监控与退化触发

| 监控项 | 指标 | 触发动作 |
|---|---|---|
| 排序能力衰减 | **分窗 PR-AUC**（非 ROC） | 超过 δ 触发告警 / 重训 |
| 概率质量退化 | 决策区间可观测失准 | 触发近窗重校准 |
| 先验漂移 | 周度欺诈率 | 记录，不单独触发 |

**实测结果（诚实的"稳定"结论）** `[实测]`：冻结模型在 6 个月滚动窗上 ROC 0.881–0.918、**0 次触发**（δ=0.02）。
**副产物洞察**：同期 **PR-AUC 波动约 0.09**，远比 ROC 敏感 → **正类监控应盯 PR-AUC，盯 ROC 会以为系统没事**。
**漂移形态判定**：聚合欺诈率稳定（≈3.5%），但周度在 **2.07%–5.07%** 间波动（≈2.4×）→ 属**波动/分布型漂移**，非先验上行。
> 说明：要展示戏剧性的衰减曲线需人为注入漂移，**未做，因为不诚实**。

---

## 10. Agent 层（调查与建议生成）

**架构**：GBDT 出分 → **闸门**（应然档为"放行"则不进 Agent）→ 检索规则/案例 → 自主工具调用（4 工具，上限 8 次）→ 结构化报告（每条结论强制 `evidence_ids`、每个事实带时间窗）。

**闸门放行率 = 90.3%** `[实测]`（四档 approve 直接放行、不进 Agent；`reports/stepup.md`，与 `demo_data.json` 的 `gate_pass_rate_four_tier=0.9029` 一致）。
五档下的**自动处置率**为 93.3%（approve + step-up），但 step-up 是自动加验证、不等于"不进 Agent"，两个口径不可互换。
> ⚠️ **数值撞车警示**：`96.5%` 是**另一个完全不同的量**——bootstrap 概率 `P(生产拓扑 < 全放行)=96.5%`。
> 它曾被误当作闸门放行率写进 README（2026-08-08 已订正）。**两个不相干的量恰好同值，是最难用肉眼发现的一类错误；引用任何数字前必须回源。**

**成本与用量** `[实测, **v1**]`：每单 **$0.1131**、平均 **5.5** 次工具调用（出处 `agent_eval_r1.md`，r1 轮）。
**闸门的三重身份**：省 LLM 成本 / 防 Agent 过度上报的行为护栏 / **在经济上结构性消灭一整类 Agent 误判**（按笔数的头号病灶在生产拓扑下贡献 $0）。

**硬层验收**（代码 + 真标签，不含 LLM 裁判）`[实测, **v1→v2**]`：结构合规 99%→100%（r1→r3）· **编造数字 0**（2,672 个数字全量两级匹配；机械三分类 `true_ungrounded` **0/565**）· 编造 evidence_id 1%→0% · **引用完整率 92.4%**（finding 级 522/565）· **时间边界泄漏审计 100%**。

> **⚠️ 口径变更记录（2026-08-06 审计）**：此前文档中的 **98.4%** 来自 round 2 的 562/571，
> 当时仅判定 9 条引用不完整；现行权威口径为机械三分类（`agent_grounding.md`）的
> **522 fully_cited / 43 citation_gap / 0 true_ungrounded / 6 no_numbers**，即 **522/565 = 92.4%**
> （若把 6 条无数字 finding 记为通过则为 528/571 = 92.5%，差 0.1pp，结论不变）。
> **旧值比新值好看 6 个百分点，且曾出现在简历级位置，已全部替换为现行值。** `[已作废: 98.4%]`
>
> **两个数必须分开报，不得合成"接地率"**：**编造率 0**（严重缺陷，实测为零）与
> **引用完整率 92.4%**（卫生问题：值真实存在于整单证据池，只是未挂到该条 finding 上）。

**职责边界（经实验确定，非设计假定）**：
- 证据层 vs 决策层（已做多数类基线校正）：**+32pp vs +11pp，净差 +21pp** `[实测, **v1**]` → **取证归 Agent、算术归公式**。
- 一次预注册、零调参的处置 prompt 修订结果为 **零**；配对迁移显示 20 笔改档、方向全对，但改对 7 / 改坏 6 / 换个错法 7 → **听见了、照做了，照做不能让它更准** → 处置权交回闭式解。
- 独立性（谄媚测试）：模型分拉高 150×，证据层翻转 **0/15**；冲突检出剂量梯度 0% → 27% → 47% → 100%，关键对照 **+73pp**（CI [+51,+96]）`[实测, **v2**]`。
- 弃权能力（受控消融）：逐级掐掉工具返回 → 弃权率 **0% → 10% → 100%** `[实测, **v3**]`。**这是能力测试，不是发生率**；正常运行下 0/100 的读数是"没机会"，不是"不会"。

---

## 11. 降级方案（⑨）

**核心风控不依赖任何外部大模型。** LLM 不可用 / 超时 / 限流时：GBDT 照常出分，报告退化为规则模板。
- 降级产物**通过同一套校验器**（结构、evidence_ids、时间边界审计），不是未经验证的第二条代码路径。
- 服务层返回 **200 + 降级标记**，不返回 5xx：**降级是产品行为，不是服务故障**，避免上游误判为故障而重试或熔断。
- **已两次被真实触发**（非模拟）`[实测]`。

---

## 12. 变更记录

| 版本 | 变更 | 影响 |
|---|---|---|
| Agent v3 | 基线证据池 | r1 / r3 / r4 全部指标的基线 |
| Agent **v4-citable-context** | 缺席事实（`null_result`）与成本假设（`policy_param`）纳入证据池并可被引用 | **证据池变更 → 跨版本指标不可直接比较**；同笔剥夺样本上编造违规 6 → 0 |

---

## 13. 已推演但未实现的工业化路径

以下为真实生产系统具备、本离线系统**未实现**的能力，列出以界定边界：

- **名单体系前置**（黑/白/灰名单）与**规则引擎硬拒**先于模型打分
- **step-up 加验证**作为第五档动作（见 §7 缺口）
- **冠军/挑战者**与灰度发布、影子模式
- 实时特征服务与延迟预算（本系统为离线批处理）
- 案件管理：队列、SLA、复核员分派、申诉通道
- 模型风险管理流程：独立验证、模型清单、定期复审

> **诚实定位重申**：本系统是**离线决策系统**。上述能力我能说清它们为什么需要、以及接进来会改变哪些结论，但**没有实现过，也不声称实现过**。
