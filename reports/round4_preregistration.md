# round 4 预注册（跑前写死，跑完不许改）

基线轮次：`r3`（100 份报告 / 571 条 finding）

## 指标定义（代码即定义，见 `src/eval/round4_metrics.py`）

1. **主指标**（越小越好）：`confirmed` + 所引 fact 全为 `txn_field:*` + 不同 fact 数 ≤2 的 finding 计数。
2. **反向护栏**（越小越好）：`tentative` + 所引 ≥2 条 `STAT_/GRAPH_/RULE_` fact 的 finding 计数。
   —— 防止「一改就全线降档」把主指标刷到 0；**两边必须同时不恶化**才算改进。
3. **次指标**：主指标命中里真属「由字段推出的判断」的条数（小样本手核，带否定/边界守卫）。

## 冻结基线

- **主指标 = 25** 条
- **反向护栏 = 34** 条
- 断言强度分布：confirmed 162、supported 351、tentative 58

## 判读规则（预先写死）

- 主指标下降**且**护栏不上升 → 记为改进；
- 主指标下降**但**护栏上升 → 记为**过度矫正**，不算改进；
- 主指标不动 → **报零**（round 3 已有先例，零也是结果）。

## 单变量纪律

round 4 **只改 prompt 一处**。`policy_param`（成本假设升为一等公民 fact）**不在 round 4 运行期内生效**——
否则证据池同时改变，主指标会因「不再全是 `txn_field`」而下降，那不是 prompt 的功劳。

## 基线命中明细（供跑后逐条对照）

- `3481073#0`：«交易金额极小（$44.27），即便为欺诈，单笔损失有限，拦截性价比低。»
- `3482791#0`：«本笔金额极小（$29），即便为欺诈其单笔损失上限也很低，单纯就本笔而言不值得拦截。»
- `3492819#4`：«交易金额较小（$200），即便存疑，拦错成本相对整笔金额有限。»
- `3494922#0`：«交易金额极小（$14.33），单笔损失敞口有限，影响处置的成本权衡。»
- `3502069#0`：«交易金额极小（$16.72），即使为欺诈最大损失也仅约$17，限制了拦截/上报的成本收益。»
- `3505282#5`：«本笔金额仅 $15.72，属极小额，单笔直接经济损失有限，处置重点应落在实体层面而非单笔拦截。»
- `3508739#0`：«交易金额极小（$28.04），单笔放过的期望损失有限，削弱了拒绝档的价值»
- `3509916#0`：«交易金额极小（$39），即使误判放行，单笔损失上限也很低。»
- `3510143#0`：«本笔金额极小（$27.05），单看金额不足以支撑高成本处置，但需结合实体证据判断。»
- `3512319#7`：«Transaction amount is very small ($12.36), so the direct loss on this single txn is negligible; valu»
- `3512816#5`：«本笔金额较小（$63.45），单笔拦截的期望收益有限，但实体层的团伙证据使冻结实体的价值远超单笔。»
- `3512903#4`：«金额较小（$36.15），单笔损失有限；但团伙证据下上报冻结实体可拦住未来批量欺诈，成本框架上小额亦值得上报。»
- `3519769#3`：«金额较小（$275），即便存疑其潜在损失有限。»
- `3522711#0`：«交易金额极小，仅 $3.44，即使为欺诈，放行的直接损失也远低于拦截成本（误拦约 $25）。»
- `3523036#5`：«Transaction amount is small ($34.92); on amount alone it would be marginal, but this is outweighed b»
- `3523094#0`：«交易金额小（$100），即使误放损失也有限。»
- `3524436#0`：«交易金额极小（$1.49），单笔放错的直接损失可忽略；风险不在金额本身，而在实体。»
- `3526727#0`：«交易金额仅 $50，为小额交易，即便放错损失也被限制在低位。»
- `3537070#0`：«交易金额极小（$26.95），即便欺诈其最大损失也远低于误拦成本，处置上更倾向放行。»
- `3537178#0`：«金额较小（$179.83），即使欺诈单笔损失有限。»
- `3537557#0`：«Transaction is very low value ($16.23), limiting maximum loss exposure even if fraudulent.»
- `3552965#5`：«本笔金额仅 $28.90，单笔损失敞口小；但团伙证据使其价值在于冻结实体以拦截未来批量欺诈，而非单笔止损。»
- `3554924#0`：«交易金额很小（$41.32），即使误判放行，单笔潜在损失也很有限。»
- `3558979#0`：«交易金额极小（$49），即使欺诈，单笔损失上限也很低。»
- `3574733#0`：«交易金额仅 $92，属小额；即便误判损失有限。»