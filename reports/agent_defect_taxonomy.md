# 缺陷分类学（owner 亲手核验，n=17）

> **这是本项目里唯一由人直接产出的证据。** 其余所有「锚」均为 Opus 4.8 生成（见 PROGRESS ⚠️ 修正节）。方案 ① 之后，证据与推理层是 Agent 的全部职责，所以这份清单不是「prompt 的输入」，而是**对 Agent 唯一那份职责的人工核验结果**。

> n=17，**一律不报比率**，只报计数、形态与原文。

## 判定汇总

- 推理不成立（reasoning_valid=false）：**1** 条
- 过度断言（overclaim=true）：**4** 条
- 逐条批注覆盖：**17/17** 条（owner 对每条都写了判据）
- `uncertain` 标记：**0** 条
- few-shot 自测得分：**未做**；改判留痕：**无**；标注耗时：**未记录**

## 形态聚类（按 owner 自己的术语，不做语义推断）

| 形态 | 条目数 | 含义 |
|---|---|---|
| 引用瑕疵（缺引但池中有据） | **7** | 值在整单证据池里有据、只是没挂在这条 finding 上 |
| 保守偏置（标低于可达档） | **5** | 断言强度**低于**证据可支撑的最高档 |
| 超档越界（断言强度高于可达档） | **4** | 断言强度**高于**证据可支撑的最高档 |
| 非因果跳跃（证据类型不足以确立主张） | **1** | 所引 fact 的**类型**根本推不出该主张 |

### ⭐ 最要紧的一条：`confirmed` 被用在「由字段推出的判断」上

4 条过度断言里有 3 条是同一形态——**引用的是单个字段值（多为金额），却把由它推出的判断标成 `confirmed`**：

- **3508739#5**（预设 `confirmed`，引用 ['TXN_000']）
  - finding：«Transaction amount is very small ($28.04), limiting worst-case loss if it is fraud.»
  - owner：预设 confirmed，但主张包含“损失有限”的因果推论，非纯字段本义引用，最高仅达 supported，超档越界
- **3510638#0**（预设 `supported`，引用 ['TXN_000', 'TXN_001', 'TXN_003', 'TXN_004', 'STAT_000', 'STAT_002']）
  - finding：«小额交易 $82，ProductCD=W（该品类历史欺诈率2.07%，低于全体基率3.5%），mastercard/debit（3.5%，基线水平），拦截误伤成本高于潜在损失。»
  - owner：预设 supported，“成本收益权衡”断言需成本证据，证据池无，实际最高仅 tentative，超档越界
- **3547972#6**（预设 `confirmed`，引用 ['TXN_000']）
  - finding：«Transaction amount is small ($35.66), which lowers per-txn loss but does not offset entity-level gang risk.»
  - owner：预设 confirmed，但“不能抵消 gang risk”需金额+gang风险双事实对比，单条金额事实最高仅 tentative，超档越界
- **3552038#0**（预设 `confirmed`，引用 ['TXN_000', 'TXN_001', 'TXN_003', 'TXN_004', 'TXN_005', 'TXN_006']）
  - finding：«交易本身为小额($81.09)、ProductCD=C、mastercard/debit、hotmail.com、移动设备——与命中的高危规则和相似案例的画像高度一致。»
  - owner：预设 confirmed，但“与画像一致”为对比关系判断，需规则/案例参照，单引本笔属性最高仅 supported，超档越界

> **可修复的形态**：字段值本身可达 `confirmed`，但**由字段推出的因果/对比判断**最高只能到 `supported`（甚至 `tentative`）。
> 这是一条**能写进 prompt 的具体规则**，而不是「请更谨慎」这种没法执行的话。

## ⚠️ owner 在批注里指出的一个 rubric 漏洞（值得单独记）

唯一一条 `reasoning_valid=false`（**3510638#0**）的批注里，owner 写道：

> 「『误伤成本>损失』需成本数据支撑，证据池缺失该类证据，属非因果跳跃。**但之前在 ml 层确实有过类似的结论，因此我怀疑是不是需要结合更大背景比如整个项目的。**」

这是一个**真实的 rubric 漏洞**，而且已经在代码里有实证：
- Agent 的 system prompt **本身就写着**四档成本参数（误拦 $25 / 复核 $5 / 上报 $40）；
- 硬层的数字对账器 `COST_CONSTS` **已经把这些常数当作合法来源**（`[0.0351, 3.51, 5.0, 21.0, 25.0, 40.0]`）；
- 但 RUBRIC_V2 要求「只依据给出的证据判断」，而**证据池里没有这些常数**。

→ **同一个成本推论，硬层判它有据、软层判它无据。**
这不是 owner 判错，是**两层的「证据」定义不一致**：
硬层把 prompt 提供的常数算进证据，软层没有。
**修法（记为待办，不在本轮改）**：rubric 需明确「prompt 内提供的成本参数属于可用前提」，或反过来把它们从硬层合法集里剔除——两者选一，但必须一致。

## 对 r1 口径的含义（金标漂移）

重标的 8 条旧条目里翻转 **2** 条，且**都只翻 overclaim 维、reasoning 维只翻 1 条**。
→ 漂移不大，**r1 的 41/35/17 可作为「跨模型对齐度」保留**，但仍不得称为「judge 对不对」的度量（锚是 Opus 4.8，非人）。

> 合并漂移口径（换标准 + 换标注者）是设计，不是缺陷——要回答的是「r1 口径还能不能往下用」，答案不取决于分解。
