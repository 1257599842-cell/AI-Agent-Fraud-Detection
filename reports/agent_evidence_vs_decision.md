# 证据层 vs 决策层拆分（方案 ① 的地基）

**问题**：Agent 在取证/定性上的水平，是否显著高于它在选处置档上的水平？

> ⚠️ 本文件对三个陷阱做了显式处理，读数时请连同处理一起读：①两层类别数不同（4 分类 vs 2 分类）→ **只看相对多数类基线的提升**；②`gang_score≥0.5` 是代码代理不是真值 → **另加 isFraud 非循环锚**；③0.5 门槛是选的 → **全门槛扫描**。


## round `r1`

### 1. 证据层：Agent `gang_association.suspected` × 代码 `gang_score≥0.5`

- **未表态（`gang_association` 为 null）3/100 笔**　⚠️ 这批被排除在下表之外

| | 代码判团伙 | 代码判非团伙 |
|---|---|---|
| **Agent 判团伙** | 40 | 9 |
| **Agent 判非团伙** | 2 | 46 |

- 原始一致率 **89%**（86/97，95% CI [81%, 94%]）
- 多数类基线 **57%**（全猜「非团伙」）
- **相对基线提升 +32pp** ← 只有这个数可以拿去和决策层比

### 2. 非循环锚：两者各自对 `isFraud` 真标签（同意≠正确）

| 判定 | n | 其中真欺诈 | 欺诈率 |
|---|---|---|---|
| Agent 判团伙 | 49 | 19 | 39% |
| Agent 判非团伙 | 48 | 12 | 25% |
| 代码 gang≥0.5 | 42 | 16 | 38% |
| 代码 gang<0.5 | 55 | 15 | 27% |
| （全体） | 97 | 31 | 32% |

- 欺诈率分离度（判团伙组 − 判非团伙组）：
  - **Agent +14pp**，95% CI [-5, +32]pp　**跨 0**
  - **代码 gang_score +11pp**，95% CI [-8, +30]pp　**跨 0**
- 读法：若两者互相同意、却都分离不出欺诈，则第 1 节那个一致率是**空的**（两个都指向同一个没用的方向也叫一致）。
- ⚠️ 两个区间都跨 0 时，**只能说「未观测到指向真实欺诈的证据」**，不能说「证明了两者都没用」——n≈100、欺诈基率 32%，本就没有功效检出这个量级的分离。

### 3. `gang_score` 门槛敏感性（0.5 是选的）

| 门槛 | 代码判团伙数 | 一致率 | 多数类基线 | 提升 |
|---|---|---|---|---|
| 0.3 | 58 | 89% | 60% | +29pp |
| 0.4 | 46 | 89% | 53% | +36pp |
| 0.5 | 42 | 89% | 57% | +32pp |
| 0.6 | 34 | 85% | 65% | +20pp |
| 0.7 | 29 | 79% | 70% | +9pp |

### 4. 把 Agent 的 `suspected` 代回 `argmin_action()` 重算处置

问的是 ① 下最要紧的一件事：**Agent 的取证错误，经公式传导后会造成多大决策偏差**。

| g 的映射 | 与应然档一致率 | 多数类基线 | 提升 |
|---|---|---|---|
| suspected→g=1.0 / 否→0 | 87% | 45% | +41pp |
| suspected→g=0.75 / 否→0 | 88% | 45% | +42pp |
| suspected→保留代码幅度 / 否→0 | 90% | 45% | +44pp |

- 对照：用**代码 gang_score** 走同一公式 = 100%（应为 100%，因为应然档就是这么算的——这行是口径自检）

## round `r3`

### 1. 证据层：Agent `gang_association.suspected` × 代码 `gang_score≥0.5`

- **未表态（`gang_association` 为 null）12/100 笔**　⚠️ 这批被排除在下表之外

| | 代码判团伙 | 代码判非团伙 |
|---|---|---|
| **Agent 判团伙** | 41 | 3 |
| **Agent 判非团伙** | 1 | 43 |

- 原始一致率 **95%**（84/88，95% CI [89%, 98%]）
- 多数类基线 **52%**（全猜「非团伙」）
- **相对基线提升 +43pp** ← 只有这个数可以拿去和决策层比

### 2. 非循环锚：两者各自对 `isFraud` 真标签（同意≠正确）

| 判定 | n | 其中真欺诈 | 欺诈率 |
|---|---|---|---|
| Agent 判团伙 | 44 | 17 | 39% |
| Agent 判非团伙 | 44 | 13 | 30% |
| 代码 gang≥0.5 | 42 | 16 | 38% |
| 代码 gang<0.5 | 46 | 14 | 30% |
| （全体） | 88 | 30 | 34% |

- 欺诈率分离度（判团伙组 − 判非团伙组）：
  - **Agent +9pp**，95% CI [-11, +29]pp　**跨 0**
  - **代码 gang_score +8pp**，95% CI [-12, +27]pp　**跨 0**
- 读法：若两者互相同意、却都分离不出欺诈，则第 1 节那个一致率是**空的**（两个都指向同一个没用的方向也叫一致）。
- ⚠️ 两个区间都跨 0 时，**只能说「未观测到指向真实欺诈的证据」**，不能说「证明了两者都没用」——n≈100、欺诈基率 32%，本就没有功效检出这个量级的分离。

### 3. `gang_score` 门槛敏感性（0.5 是选的）

| 门槛 | 代码判团伙数 | 一致率 | 多数类基线 | 提升 |
|---|---|---|---|---|
| 0.3 | 56 | 84% | 64% | +20pp |
| 0.4 | 45 | 92% | 51% | +41pp |
| 0.5 | 42 | 95% | 52% | +43pp |
| 0.6 | 34 | 89% | 61% | +27pp |
| 0.7 | 29 | 83% | 67% | +16pp |

### 4. 把 Agent 的 `suspected` 代回 `argmin_action()` 重算处置

问的是 ① 下最要紧的一件事：**Agent 的取证错误，经公式传导后会造成多大决策偏差**。

| g 的映射 | 与应然档一致率 | 多数类基线 | 提升 |
|---|---|---|---|
| suspected→g=1.0 / 否→0 | 88% | 47% | +41pp |
| suspected→g=0.75 / 否→0 | 88% | 47% | +41pp |
| suspected→保留代码幅度 / 否→0 | 90% | 47% | +43pp |

- 对照：用**代码 gang_score** 走同一公式 = 100%（应为 100%，因为应然档就是这么算的——这行是口径自检）

---

## 并排对比（**只比相对基线的提升**）

| round | 层 | 原始一致率 | 多数类基线 | **相对基线提升** |
|---|---|---|---|---|
| r1 | 证据层（2 分类） | 89% | 57% | **+32pp** |
| r1 | 决策层（4 分类） | 57% | 45% | **+11pp** |
| r3 | 证据层（2 分类） | 95% | 52% | **+43pp** |
| r3 | 决策层（4 分类） | 59% | 47% | **+13pp** |

> 原始一致率那两列**不能直接比**（4 分类 vs 2 分类），列出来只是为了让读者看到「不校正会得出什么」。可比的是最后一列。


## ⚠️ 自查：未表态率上升会不会把一致率「洗高」

`r3` 的未表态从 `r1` 的 3 笔涨到 12 笔，而**未表态的样本被排除在一致率分母之外**。
若它恰好在难判的那些笔上弃权，剩下的一致率就会**凭选择效应变好**（⑤ 选择性偏差在证据层的翻版：用自己的弃权决定分母）。

- `r3` 弃权的 12 笔中，有 11 笔 `r1` 曾给出结论；
  **`r1` 在这批上的一致率 = 100%**（vs `r1` 全体 89%）

  → **不低于全体 → 没有证据表明它挑难的弃权**，`r3` 的一致率上升不是弃权洗出来的。
- 最坏口径（未表态一律算错）：`r1` 证据层一致率 89% → **86%**
- 最坏口径（未表态一律算错）：`r3` 证据层一致率 95% → **84%**

## 裁定（分两句，别合成一句）

### (a) 能力层面：Agent 复现「团伙判定」这件事的水平 —— **成立**

✅ **证据层相对基线的提升显著高于决策层**（+32pp vs +11pp，差 **+21pp**）。
校正了类别数差异后仍然成立；门槛 0.3–0.6 区间稳定；未表态的选择效应已自查排除；最坏口径（弃权算错）下仍是 84–86%。
→ **「取证的活 Agent 干得比选档的活好」有数据支撑。**

### (b) 效度层面：「团伙判定」这件事本身有没有用 —— **未测出**

- Agent 的团伙判定对 isFraud 的分离度 **+14pp**，95% CI [-5, +32]pp（**跨 0**）
- 代码 gang_score 的分离度 **+11pp**（同样跨 0）

**所以第 (a) 句必须带着第 (b) 句一起讲**：
> Agent 能高度可靠地复现代码的团伙判定（+32pp 提升），**但「这个团伙判定指不指向真欺诈」在本数据上没有测出来**——
> 两个分离度的置信区间都跨 0。一致率只证明两个判定**互相同意**，不证明它们**对**。

**但也不能就此说 gang 没用，因为这个锚测的不是它的设计目标**：
- `gang_score` 进入四档公式的位置是**上报档的网络项** （`− p·g·k_future·a_med`），它要预测的是**冻结该实体能拦下的未来欺诈**，
  不是**本笔是否欺诈**。拿本笔 `isFraud` 当锚，测的是一个它没打算预测的目标。
- **对的锚应该是**：gang_score 高的实体，其**后续**交易的欺诈率是否更高（同实体、时间窗往后推）。这个可算但没算，**记为未完成，不拿现在的跨 0 结果当结论**。

- 未表态率：r1 3/100、r3 12/100（`gang_association=null`）。自查显示弃权集中在**容易**的样本上，不构成选择效应。

### 对 ① 的含义

① **可以做**，理由是 (a)：职责边界有能力层面的数据支撑，不再只是推演。
对外表述必须是：
> 「我用数据划了职责边界——同一批报告里，Agent 复现团伙判定的水平（相对基线 +32pp）明显高于它选处置档的水平（+11pp），
> 所以把决策交回闭式解、让 Agent 专做取证。**其中团伙判定本身的效度我另做了检验，在这批数据上没测出来，正确的锚是实体的未来欺诈率，那个我没跑。**」
