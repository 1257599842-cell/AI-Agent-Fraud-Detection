# round 4 结果（对照预注册）

| 指标 | r3（基线） | r4 | 变化 |
|---|---|---|---|
| **主指标**（confirmed+纯字段+≤2 fact） | 25 | **9** | -16 |
| **反向护栏**（tentative+≥2 统计/图 fact） | 34 | **26** | -8 |
| finding 总数 | 571 | 644 | +73 |

**断言强度分布**（看有没有全线降档）：

| 强度 | r3 | r4 |
|---|---|---|
| confirmed | 162 | 152 |
| supported | 351 | 452 |
| tentative | 58 | 40 |

## 判读（按预注册规则，不改口径）

✅ **记为改进**：主指标 -16，护栏 -8（未恶化）。

## ⚠️ 事后分析：预注册的护栏有一个盲区（照实记，**不改指标**）

护栏当初是按「怕它全线降档到 tentative 刷分」设计的。**实际位移不在那个方向**：

| 强度 | r3 占比 | r4 占比 | 变化 |
|---|---|---|---|
| confirmed | 28.4% | 23.6% | -4.8pp |
| supported | 61.5% | 70.2% | +8.7pp |
| tentative | 10.2% | 6.2% | -3.9pp |

- 质量**全部涌向 supported**（61.5% → 70.2%，+8.7pp），
  且**两头都在流入**：confirmed -4.8pp、tentative -3.9pp。
- `confirmed → supported` **正是 prompt 要的**；但 `tentative → supported` 是**升档**，prompt 没要求过，属于计划外的副作用。
- **我的护栏看不见这个方向**：它只统计 `tentative + ≥2 统计/图 fact`，所以「大家都往 supported 收敛」时它反而会变好看。
  护栏这次的下降（−8）**有一部分正是 tentative 总数变少带来的**，不能全记作「没有过度矫正」。

> **这是预注册本身的缺陷，不是结果的缺陷**：单边护栏只挡住了我预想的那一种作弊方式。
> 按规矩**指标不改、判读不改**；但「记为改进」这句话必须带着这一条一起讲。
> 正确的护栏应当是**双边**的——同时盯「降档」与「升档」，或者直接盯强度分布的整体位移。
- 另记：finding 总数 571 → 644（+12.8%），主指标**率** 4.38% → 1.40%（率的降幅比绝对计数更大，方向一致）。

## 次指标：主指标命中里真属「由字段推出的判断」的条数

（带否定/边界守卫；本项目此类错误已栽三次，故守卫与计数一起给出）


## 补测：`assertion_strength` 各档的证据画像（描述性，指标不改）

问的是一个比「护栏是单边的」更要紧的问题：**修完之后，断言强度这个字段还分得开吗？**

| 轮次 | 强度 | n | 平均引用 fact 数 | 引用统计/图类 fact 占比 | 只引 txn_field 占比 |
|---|---|---|---|---|---|
| r3 | confirmed | 162 | 2.99 | 72% | 24% |
| r3 | supported | 351 | 3.82 | 79% | 0% |
| r3 | tentative | 58 | 3.38 | 79% | 2% |
| r4 | confirmed | 152 | 4.47 | 39% | 59% |
| r4 | supported | 452 | 3.39 | 80% | 3% |
| r4 | tentative | 40 | 3.58 | 75% | 0% |

- **强度序数 ↔ 引用 fact 数 的 Spearman 相关**：`r3` **-0.178** → `r4` **+0.193**
- confirmed 与 tentative 的平均引用数之差：`r3` -0.39 → `r4` +0.90

> ✅ **预先写好的「变钝」那一支没有触发；实际结果比它更强：关联的方向被修正了。**

> `r3` 的相关是 **-0.178（负的）**——`confirmed` 引用的 fact 数反而**少于** `tentative`（2.99 vs 3.38）。
> **强度与证据量呈反向关系，这本身就是那个缺陷的独立证据**：「拿一个字段值就敢标 confirmed」正是它的成因。
> `r4` 修正为 **+0.193（正的）**，confirmed 的平均引用数升到 4.47、高于 tentative 的 3.58。

> 另一处佐证：`r4` 的 confirmed 里「只引 txn_field」占比反而升到 59%（`r3` 为 24%）——
> **这与主指标下降并不矛盾，方向恰好一致**：prompt 明说「字段值本身可以标 confirmed」，
> 所以「罗列多个字段值」留在 confirmed 是**对的**；被赶走的是「**只引 1–2 个字段却下因果/对比判断**」那一类（主指标正是这么定义的）。

> → 强度字段**没有变钝，反而变准了**。主指标的下降不是靠把它挤扁换来的。

> **通则（本项目第三次被单边指标坑）**：judge 的过严率（只测过严、测不了过松）、
> 富集筛子（只放大 judge 认为有问题的）、本轮的预注册护栏（只防降档、不防升档）。
> → **护栏必须双向；只防一个方向的过度矫正等于没防。**

- `r3`：主指标 25 条中，判为「由字段推出判断」**9** 条
  - `3481073#0`：«交易金额极小（$44.27），即便为欺诈，单笔损失有限，拦截性价比低。»
  - `3502069#0`：«交易金额极小（$16.72），即使为欺诈最大损失也仅约$17，限制了拦截/上报的成本收益。»
  - `3505282#5`：«本笔金额仅 $15.72，属极小额，单笔直接经济损失有限，处置重点应落在实体层面而非单笔拦截。»
  - `3508739#0`：«交易金额极小（$28.04），单笔放过的期望损失有限，削弱了拒绝档的价值»
  - `3512903#4`：«金额较小（$36.15），单笔损失有限；但团伙证据下上报冻结实体可拦住未来批量欺诈，成本框架上小额亦值得上报。»
  - `3519769#3`：«金额较小（$275），即便存疑其潜在损失有限。»
- `r4`：主指标 9 条中，判为「由字段推出判断」**2** 条
  - `3512816#0`：«交易金额仅 $63.45，属小额交易，单笔放错的直接损失有限。»
  - `3544856#0`：«交易金额仅 $13.03，属极小额；单笔拦截/放行的直接损失有限。»